let existingUsers =["Admin","Eric","Taha","Munir","Shoukat"];
let newUser="Admin";
// for (let allusers of newUser)
if(existingUsers.indexOf(newUser)!==-1)
{
    console.log (`User name already exists`)
}
else
{
    console.log(`User name available`)
};

